---
applications:
  .:
    name: tekken
    framework:
      name: wsgi
      info:
        mem: 64M
        description: Python WSGI Application
        exec: 
    url: ${name}.${target-base}
    mem: 64M
    instances: 1
    services:
      mongodb-tekken:
        type: mongodb
      redis-tekken:
        type: redis
